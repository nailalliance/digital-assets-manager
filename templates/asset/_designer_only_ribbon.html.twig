{% if asset.status and asset.status.value == 'designer' %}
    <div class="absolute top-0 left-0 z-20">
        <span class="inline-flex items-center rounded-br-xl bg-sky-600 px-3 py-1.5 text-[10px] font-bold uppercase tracking-[0.2em] text-white shadow-lg">
            Designer Only
        </span>
    </div>
{% endif %}
