<form method="post" action="{{ path('admin_brands_delete', {'id': brand.id}) }}"
      onsubmit="return confirm('Are you sure you want to delete this item?');" class="inline">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ brand.id) }}">
    <button class="text-red-600 hover:text-red-900">Delete</button>
</form>
